{
  "$id": "https://developer.microsoft.com/json-schemas/fabric/item/ontology/resourceLinks/1.0.0/schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "resource links definition",
  "type": "object",
  "properties": {
    "$schema": {
      "type": [
        "string",
        "null"
      ]
    },
    "resourceLinks": {
      "type": "array",
      "description": "List of resource links",
      "items": {
        "$ref": "#/definitions/ResourceLink"
      }
    }
  },
  "required": ["resourceLinks"],
  "definitions": {
    "ResourceLink": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["PowerBIReport"],
          "description": "Type of resource link"
        },
        "workspaceId": {
          "type": "string",
          "format": "uuid",
          "description": "The id of the workspace where the customer’s resource link is located"
        },
        "itemId": {
          "type": "string",
          "format": "uuid",
          "description": "ArtifactId of the customer’s resource link"
        }
      },
      "required": ["type", "workspaceId", "itemId"]
    }
  }
}
